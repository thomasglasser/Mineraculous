package dev.thomasglasser.mineraculous.api.client.gui.selection;

/// Handles the closing event of a {@link SelectionMenu}.
public interface SelectionMenuListener {
    void onMenuClosed(SelectionMenu menu);
}
